# Memact Influence Benchmarks

These fixtures are fake but realistic. They test whether Memact can prefer evidence-backed influence paths over fluent answers.

Run:

```bash
npm run benchmarks
```

The runner checks repeated exposure, thought-before-exposure, competing origins, unknown origin, phrase-overlap traps, semantic matches, survey-only context, and sensitive-source exclusion.
